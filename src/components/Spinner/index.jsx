// Spinner.js
import React from "react";
import "./Spinner.css"; // You can add styles for the spinner in a separate CSS file

const Spinner = () => {
  return <div className="spinner"></div>;
};

export default Spinner;

